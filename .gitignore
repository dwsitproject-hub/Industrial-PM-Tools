# dependencies & build output
node_modules/
backend/dist/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# environment files — never commit secrets
backend/.env
backend/.env.staging
*.env.local

# database dumps & legacy data export (real business data + legacy plaintext passwords)
deploy/
db/

# generated reports
backend/prisma/renumbered_tickets_report.csv

# OS / editor noise
Thumbs.db
.DS_Store

# dumps copied onto servers inside the clone
dump/
